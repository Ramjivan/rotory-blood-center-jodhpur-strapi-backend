'use strict';

/**
 * processing-charge controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::processing-charge.processing-charge');
